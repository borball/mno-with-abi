apiVersion: config.openshift.io/v1
kind: OperatorHub
metadata:
  name: cluster
spec:
  sources:
  - disabled: {% if container_registry is defined and 'redhat-marketplace' not in container_registry.catalog_sources.defaults %}true{% else %}false{% endif %}
    name: redhat-marketplace
  - disabled: {% if container_registry is defined and 'community-operators' not in container_registry.catalog_sources.defaults %}true{% else %}false{% endif %}
    name: community-operators
  - disabled: {% if container_registry is defined and 'redhat-operators' not in container_registry.catalog_sources.defaults %}true{% else %}false{% endif %}
    name: redhat-operators
  - disabled: {% if container_registry is defined and 'certified-operators' not in container_registry.catalog_sources.defaults %}true{% else %}false{% endif %}
    name: certified-operators
